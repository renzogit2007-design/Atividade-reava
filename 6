programa
{
	funcao inicio()
	{
		inteiro total, horas, minutos, segundos

		leia(total)

		horas = total / 3600
		minutos = (total % 3600) / 60
		segundos = (total % 3600) % 60

		escreva(horas, "h ")
		escreva(minutos, "min ")
		escreva(segundos, "s")
	}
}
