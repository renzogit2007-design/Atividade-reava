programa
{
	funcao inicio()
	{
		caracter sexo
		real altura, peso

		leia(sexo)
		leia(altura)

		se(sexo == 'M' ou sexo == 'm')
		{
			peso = (72.7 * altura) - 58
		}
		senao
		{
			peso = (62.1 * altura) - 44.7
		}

		escreva("Peso ideal = ", peso)
	}
}
